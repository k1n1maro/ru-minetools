# 🚀 RU-MINETOOLS - Полное руководство разработчика

## 📋 КРАТКАЯ СУТЬ ПРОЕКТА

**RU-MINETOOLS** - утилита для автоматического перевода модов и квестов Minecraft на русский язык с современным игровым интерфейсом и системой автообновлений.

---

## 🎯 ЧТО ДЕЛАЕТ ПРОГРАММА

### Основные функции:
1. **Переводит FTB квесты** - файлы `.snbt` в папках `chapters/` и `lang/`
2. **Переводит JAR моды** - языковые файлы внутри `.jar` архивов
3. **Система авторизации** - через Telegram бота или гостевой доступ
4. **Автообновления** - скачивает новые версии с GitHub и заменяет EXE файл
5. **Кэширование** - сохраняет переводы для ускорения повторной работы

### Технологии:
- **PyQt6** - современный GUI интерфейс
- **translatepy** - API переводчика
- **Telegram Bot API** - авторизация пользователей
- **Yandex Cloud** - серверная часть авторизации
- **GitHub Releases** - система обновлений
- **PyInstaller** - сборка в EXE файл

---

## 📁 СТРУКТУРА ПРОЕКТА (ФИНАЛЬНАЯ ВЕРСИЯ 1.0.0)

```
ru-minetools/
├── 🐍 src/                        # ИСХОДНЫЙ КОД (7 файлов)
│   ├── modern_gui_interface.py    # 👑 ГЛАВНЫЙ ФАЙЛ - весь интерфейс (12,000+ строк)
│   ├── modern_updater.py          # 🔄 Система обновлений
│   ├── modern_update_overlays.py  # 🎨 UI диалоги обновлений
│   ├── update_notifications.py    # 🚨 Кастомные диалоги ошибок (восстановлено)
│   ├── translate_jar_simple.py    # 📦 Переводчик JAR модов
│   ├── enhanced_translator.py     # 🚀 Улучшенный переводчик с терминологией
│   └── utils.py                   # 🛠️ Утилиты для ресурсов

├── 🎨 assets/                     # РЕСУРСЫ (22 файла)
│   ├── icons/                     # 📁 Иконки приложения (9 файлов)
│   │   ├── app_icon.ico           # Основная иконка для EXE
│   │   ├── icon.ico               # Альтернативная иконка
│   │   ├── simple_icon.ico        # Упрощенная иконка
│   │   ├── app_icon_*.png         # PNG версии (16, 24, 32, 48, 64)
│   │   └── README.md              # Инструкции по иконкам
│   ├── logo.png                   # Логотип программы
│   ├── logow.jpg                  # Логотип для диалогов
│   ├── sans3.ttf                  # 🔤 Шрифт интерфейса
│   ├── 1.png - 5.png              # Иконки навигации (5 файлов)
│   ├── *_purple_32.png            # Фиолетовые версии иконок (5 файлов)
│   ├── *_white_32.png             # Белые версии иконок (5 файлов)
│   └── upd.png, git.png, sup.png  # Иконки кнопок (3 файла)

├── ⚙️ config/                     # КОНФИГУРАЦИЯ (8 файлов)
│   ├── 🔒 bot_config.json         # СЕКРЕТНО! Токены бота (НЕ в Git)
│   ├── 📄 bot_config.example.json # Пример настроек (В Git)
│   ├── bot_responses.json         # Тексты сообщений бота
│   ├── 🔒 guest_access.json       # СЕКРЕТНО! Данные доступа (НЕ в Git)
│   ├── 📄 guest_access.example.json # Пример доступа (В Git)
│   ├── beta_warning.json          # Предупреждения бета-версии
│   ├── minecraft_terms.json       # Словарь терминов Minecraft
│   ├── translation_quality.json   # Настройки качества переводов
│   └── update_config.py           # Настройки обновлений

├── 🔧 scripts/                    # СКРИПТЫ СБОРКИ (6 файлов)
│   ├── build_simple_exe.py        # 👑 ГЛАВНЫЙ скрипт сборки
│   ├── create_icons.py            # Создание иконок
│   ├── clear_all_auth.py          # Очистка авторизации
│   ├── fix_css_warnings.py        # Исправление CSS
│   └── clean_project.py           # Очистка проекта от мусора

├── 🎁 releases/                   # РЕЛИЗЫ
│   ├── ru-minetools-v1.0.0.exe    # EXE файл версии 1.0.0 (45.4 МБ)
│   ├── ru-minetools-v1.0.0.zip    # ZIP архив версии 1.0.0 (45.0 МБ)
│   └── README.md                  # Инструкции по релизам

├── 📚 docs/                       # ДОКУМЕНТАЦИЯ
├── 📖 README.md                   # Главное описание проекта
├── 📖 DEVELOPER_GUIDE.md          # ЭТО РУКОВОДСТВО
├── ⚠️ CRITICAL_FILES.md           # Что НИКОГДА НЕ УДАЛЯТЬ
├── 🚀 run.py                      # ЗАПУСК ПРОГРАММЫ
├── 📋 requirements.txt            # Зависимости Python
├── 🚫 .gitignore                  # Что НЕ загружать в Git
└── ⚖️ LICENSE                     # Лицензия MIT
```

### 📊 Статистика проекта v1.0.0:
- **Всего файлов**: ~50 (без .git и временных)
- **Строк кода**: ~16,000+
- **Основной код**: 7 файлов в `src/`
- **Ресурсы**: 22 файла в `assets/` (включая 9 иконок)
- **Конфигурация**: 9 файлов в `config/`
- **Скрипты**: 5 файлов в `scripts/`
- **Документация**: 4 MD файла
- **Релизы**: 3 файла в `releases/` (EXE + ZIP + README)

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. Клонирование и запуск:
```bash
git clone https://github.com/YOUR_USERNAME/ru-minetools.git
cd ru-minetools
pip install -r requirements.txt
python run.py
```

### 2. Настройка бота (если нужно):
```bash
cp config/bot_config.example.json config/bot_config.json
# Отредактировать config/bot_config.json с вашими токенами
```

### 3. Сборка EXE:
```bash
python scripts/build_simple_exe.py
```

---

## 🔒 БЕЗОПАСНОСТЬ - ЧТО ГРУЗИТЬ В GIT А ЧТО НЕТ

### ✅ ЗАГРУЖАТЬ В GIT (45 файлов):
```
src/                           # Весь исходный код (7 файлов)
├── modern_gui_interface.py    # Главный файл
├── modern_updater.py          # Система обновлений
├── modern_update_overlays.py  # UI диалоги
├── update_notifications.py    # Кастомные диалоги ошибок
├── translate_jar_simple.py    # Переводчик JAR
├── enhanced_translator.py     # Улучшенный переводчик
└── utils.py                   # Утилиты

assets/                        # Все ресурсы (22 файла)
├── icons/                     # Иконки (9 файлов)
├── *.png, *.jpg              # Иконки и логотипы
└── sans3.ttf                 # Шрифт

config/                        # Публичная конфигурация (7 файлов)
├── bot_config.example.json    # Примеры настроек
├── bot_responses.json         # Тексты сообщений
├── guest_access.example.json  # Пример доступа
├── beta_warning.json          # Предупреждения
├── minecraft_terms.json       # Словарь терминов
├── translation_quality.json   # Настройки качества
└── update_config.py           # Настройки обновлений

scripts/                       # Скрипты сборки (5 файлов)
releases/README.md             # Инструкции релизов

README.md                      # Описание проекта
DEVELOPER_GUIDE.md             # Руководство разработчика
CRITICAL_FILES.md              # Что не удалять
run.py                         # Запуск программы
requirements.txt               # Зависимости
.gitignore                     # Исключения
LICENSE                        # Лицензия
```

### 🚫 НЕ ЗАГРУЖАТЬ В GIT (автоматически исключены):
```
# Секретные конфигурации
config/bot_config.json         # ТОКЕНЫ БОТА!
config/guest_access.json       # Данные авторизации

# Пользовательские данные
telegram_auth.json             # Авторизация пользователя
src/telegram_auth.json         # Дубликат

# Кэш и временные файлы
translation_cache.pkl          # Кэш переводов
translation_cache_optimized.db # База переводов
*.log                          # Лог файлы
__pycache__/                   # Кэш Python

# Файлы сборки
*.exe, *.zip                   # Скомпилированные файлы (кроме releases/)
build/, dist/                  # Временные папки PyInstaller
*.spec                         # Спецификации сборки

# Обновления
update_ru_minetools.bat        # Скрипты обновления
*.backup                       # Резервные копии
```

---

## 🔧 КЛЮЧЕВЫЕ ФАЙЛЫ И ИХ НАЗНАЧЕНИЕ

### 👑 Самые важные файлы:

1. **`src/modern_gui_interface.py`** (12,000+ строк)
   - **Весь интерфейс программы** - единственный главный файл
   - Авторизация через Telegram (встроенный класс `TelegramAuthOverlay`)
   - Переводчик квестов и модов
   - Система настроек и проверка обновлений
   - **Импортирует**: все остальные модули

2. **`src/translate_jar_simple.py`** (900+ строк)
   - **Переводчик JAR модов** с многопоточностью
   - Кэширование переводов в pickle файлах
   - Поддержка Lang и Patchouli файлов

3. **`src/modern_updater.py`** (1,500+ строк)
   - **Система автообновлений** с GitHub Releases
   - Загрузка и замена EXE файлов
   - Batch скрипты для обновления

4. **`src/modern_update_overlays.py`** (800+ строк)
   - **UI диалоги обновлений** с современным дизайном
   - Прогресс-бары загрузки
   - Уведомления о доступных обновлениях

5. **`src/update_notifications.py`** (400+ строк) - **ВОССТАНОВЛЕНО!**
   - **Кастомные диалоги ошибок** с прозрачным фоном
   - Анимированные кнопки `HoverLiftButton`
   - Блюр эффекты и стили в духе приложения

6. **`src/enhanced_translator.py`** (600+ строк)
   - **Улучшенный переводчик** с терминологией Minecraft
   - Контекстные переводы
   - Словарь специальных терминов

7. **`src/utils.py`** (200+ строк)
   - **Утилиты для ресурсов** - критически важен для загрузки assets
   - Функции `get_asset_path()`, `get_resource_path()`, `get_config_path()`
   - Поддержка как обычного запуска, так и EXE режима

8. **`scripts/build_simple_exe.py`** (400+ строк)
   - **Главный скрипт сборки** EXE файла
   - Настройки PyInstaller с включением всех ресурсов
   - Автоматическое определение зависимостей

9. **`config/update_config.py`** (100+ строк)
   - **Конфигурация обновлений** - версия, репозиторий, настройки
   - `CURRENT_VERSION = "1.0.0"`, `GITHUB_REPO`, `UPDATE_SETTINGS`

10. **`run.py`** (50 строк)
    - **Единственная точка входа** в программу
    - Настройка путей импорта и запуск главного файла

---

## 🤖 СИСТЕМА АВТОРИЗАЦИИ

### Архитектура:
```
Пользователь → RU-MINETOOLS → Yandex Cloud → Telegram Bot
                    ↓
              Проверка подписки на канал
                    ↓
              Выдача кода авторизации
```

### Настройки:
- **Telegram Bot**: `@ru_minetools_auth_bot`
- **Канал**: `https://t.me/ruquestbook`
- **Yandex Cloud API**: `https://d5dq2g7pcv53nkqcsp1p.svoluuab.apigw.yandexcloud.net/check/{code}`

### Гостевой доступ:
- Если сервисы недоступны - предлагается гостевой режим
- Ограничен по времени (24 часа)
- Сохраняется в `guest_access.json`

---

## 🔄 СИСТЕМА ОБНОВЛЕНИЙ

### Как работает:
1. При запуске проверяет GitHub Releases API
2. Сравнивает версии (текущая vs последняя)
3. Если есть обновление - показывает **кастомный диалог**
4. Скачивает новый EXE файл
5. Создает batch скрипт для замены
6. Закрывает программу и запускает скрипт
7. Скрипт удаляет старый EXE и создает новый с версионным именем
8. Запускает новую версию

### Кастомные диалоги:
- **`update_notifications.py`** - красивые диалоги ошибок
- **`modern_update_overlays.py`** - диалоги обновлений
- Прозрачный фон с блюром
- Анимированные кнопки
- Стили в духе приложения

### Настройки:
- **Репозиторий**: `YOUR_USERNAME/ru-minetools` (настроить после создания)
- **Текущая версия**: `1.0.0`
- **Интервал проверки**: 24 часа

---

## 📦 СИСТЕМА ПЕРЕВОДОВ

### 🎯 Переводчик JAR модов (`translate_jar_simple.py`):
- **Многопоточная обработка** - 4-6 потоков для максимальной скорости
- **5-строчные батчи** для частых обновлений прогресса
- **Умное кэширование** - pickle файлы для быстрого доступа
- **Синхронизация прогресса** - исправлены "прыжки" прогресс-бара
- **Обновления каждый процент** - плавное отображение прогресса
- **Поддержка Lang и Patchouli** файлов
- **Безопасные настройки API** - предотвращение блокировки

### 🚀 Улучшенный переводчик (`enhanced_translator.py`):
- **Терминология Minecraft** - словарь специальных терминов
- **Контекстные переводы** - учет контекста при переводе
- **Качественные настройки** - фильтрация и улучшение переводов
- **🔵 Исключение синих названий модов** - §9 (blue) и §1 (dark_blue) НЕ переводятся
- **📝 Исключение названий модов** - список из 50+ известных модов остается на английском
- **Умная фильтрация цветов** - другие цвета (красный, зеленый, желтый) переводятся
- **Исключение форматирования** - жирный, курсив, подчеркивание остаются без перевода

### 🔄 Переводчик квестов (встроен в `modern_gui_interface.py`):
- **FTB Quests** (.snbt файлы)
- **Папки chapters/ и lang/**
- **Многопоточная обработка**
- **Сохранение форматирования** (цветовые коды)
- **🔵 Исключение синих названий модов** - §9 и §1 остаются на английском
- **📝 Исключение названий модов** - известные моды не переводятся
- **Поддержка цветного текста** - красный, зеленый, желтый переводятся

---

## 🎨 ИНТЕРФЕЙС И РЕСУРСЫ

### Стиль:
- Игровой дизайн в стиле Minecraft
- Темная тема с фиолетовыми акцентами
- Анимации и hover эффекты
- Кастомные кнопки и диалоги

### Шрифт:
- `assets/sans3.ttf` - основной шрифт интерфейса
- Загружается через `QFontDatabase`

### Иконки:
- Навигация: `1.png - 5.png`
- Цветные версии: `*_purple_32.png`, `*_white_32.png`
- Кнопки: `upd.png`, `git.png`, `sup.png`

---

## 🔧 СБОРКА И РАЗВЕРТЫВАНИЕ

### Подготовка к сборке:

1. **Создание иконок** (если нужно):
   ```bash
   python scripts/create_icons.py
   ```

2. **Проверка зависимостей**:
   ```bash
   pip install -r requirements.txt
   pip install pyinstaller
   ```

### Сборка EXE:
```bash
python scripts/build_simple_exe.py
```

### Что включается в EXE:
- Все файлы из `src/` (7 модулей)
- Все ресурсы из `assets/` (включая иконки)
- Публичная конфигурация из `config/`
- Секретные файлы `bot_config.json` и `guest_access.json`
- SSL сертификаты для HTTPS запросов
- Скрытые импорты Python модулей
- Иконка приложения (`assets/icons/app_icon.ico`)

### Размер EXE: ~45.4 МБ

### Создание релиза:
1. Обновить версию в `config/update_config.py`
2. Создать иконки: `python scripts/create_icons.py`
3. Собрать EXE: `python scripts/build_simple_exe.py`
4. EXE файлы сохраняются в папку `releases/`
5. Создать релиз на GitHub и загрузить ZIP файлы

---

## 🐛 ОТЛАДКА И ТЕСТИРОВАНИЕ

### Запуск из исходного кода:
```bash
python run.py
```

### Проверка ресурсов:
- Все пути к файлам через `utils.py`
- Функция `get_asset_path()` для ресурсов
- Поддержка как обычного запуска, так и EXE

### Логирование:
- Консольный вывод для отладки
- Подробные сообщения в процессе перевода
- Статус системы обновлений
- Файлы логов: `ru_minetools.log`, `ru_minetools_errors.log`

---

## ⚠️ ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### 1. Не загружаются ресурсы:
- Проверить пути в `utils.py`
- Убедиться что файлы в `assets/`
- Проверить `get_asset_path()`

### 2. Ошибки авторизации:
- Проверить `config/bot_config.json`
- Проверить доступность Yandex Cloud
- Использовать гостевой доступ

### 3. Не работают обновления:
- Проверить интернет соединение
- Проверить GitHub API
- Проверить права записи в папку

### 4. Ошибки сборки EXE:
- Установить все зависимости
- Проверить PyInstaller
- Проверить пути к ресурсам

### 5. Не показываются кастомные диалоги:
- Проверить наличие `update_notifications.py`
- Проверить импорты в `modern_updater.py`
- Убедиться что файл включен в сборку

---

## 🚀 РАЗВЕРТЫВАНИЕ НА GITHUB

### 1. Подготовка:
```bash
# Убедиться что секретные файлы исключены
git status | grep bot_config.json  # Должно быть пусто

# Проверить .gitignore
cat .gitignore | grep bot_config.json  # Должно быть
```

### 2. Коммит:
```bash
git add .
git commit -m "🎉 Initial commit: RU-MINETOOLS v1.0.0"
git push origin main
```

### 3. Настройка репозитория:
- Description: `🎮 Утилита для перевода модов и квестов Minecraft на русский язык`
- Topics: `minecraft`, `translation`, `mods`, `ftb-quests`, `russian`, `pyqt6`
- Включить Issues, Wiki, Releases

### 4. Первый релиз:
- Tag: `v1.0.0`
- Title: `🎉 RU-MINETOOLS v1.0.0 - Первый публичный релиз`
- Собрать и загрузить EXE файл из `releases/`

### 5. Обновить настройки:
- В `config/update_config.py` изменить `GITHUB_REPO` на ваш репозиторий
- Пересобрать EXE с правильными настройками

---

## 📞 КОНТАКТЫ И ПОДДЕРЖКА

### Для пользователей:
- GitHub Issues - публичные вопросы
- Telegram канал: https://t.me/ruquestbook

### Для разработчиков:
- Этот файл - основная документация
- `CRITICAL_FILES.md` - что не удалять
- Код хорошо комментирован

---

## 🎯 ЗАКЛЮЧЕНИЕ

**RU-MINETOOLS v1.0.0** - это полнофункциональная утилита с современным интерфейсом, системой авторизации, автообновлениями и **восстановленными кастомными диалогами**.

### ✨ Ключевые особенности v1.0.0:
- ✅ **Готов к продакшну** - чистая структура, актуальная документация
- ✅ **Безопасен для публикации** - правильный .gitignore, секреты исключены
- ✅ **Хорошо документирован** - подробные руководства для пользователей и разработчиков
- ✅ **Легко настраивается** - примеры конфигураций и инструкции
- ✅ **Активно поддерживается** - регулярные обновления и исправления
- ✅ **Кастомные диалоги восстановлены** - красивые уведомления и ошибки
- ✅ **Оптимизированная производительность** - плавные прогресс-бары и быстрые переводы
- ✅ **Финальная архитектура** - 7 модулей, все системы работают

### 🎉 Что нового в v1.0.0:
- **Восстановлены кастомные диалоги** из `update_notifications.py`
- **Очищена структура проекта** - удалены временные и тестовые файлы
- **Обновлена документация** - актуальная информация о всех модулях
- **Готовые EXE файлы** - в папке `releases/` с инструкциями
- **🔵 Исключение синих названий модов** - §9 и §1 остаются на английском
- **📝 Исключение названий модов** - 50+ известных модов не переводятся
- **Добавлена версия в заголовок** - теперь показывается "RU-MINETOOLS v1.0.0"
- **Финальная версия** - готова к публикации на GitHub

**⚠️ ВАЖНО**: Обязательно прочитайте `CRITICAL_FILES.md` перед любыми изменениями в проекте!

---

*Последнее обновление: 25 декабря 2025*
*Версия документации: 5.0 - Финальная версия 1.0.0 с восстановленными диалогами*