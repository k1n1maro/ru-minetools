# 📁 Папка конфигурации

## 🔧 Настройка для разработчиков

### 1. Настройка бота Telegram

Скопируйте файл примера и заполните своими данными:
```bash
cp bot_config.example.json bot_config.json
```

Отредактируйте `bot_config.json`:
```json
{
  "BOT_TOKEN": "ваш_токен_бота_от_BotFather",
  "CHANNEL_ID": "ID_вашего_канала",
  "test_date": "тестовые_данные"
}
```

### 2. Получение токена бота

1. Напишите @BotFather в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте полученный токен
4. Вставьте в `bot_config.json`

### 3. Получение ID канала

1. Создайте канал в Telegram
2. Добавьте бота как администратора
3. Отправьте сообщение в канал
4. Используйте API для получения ID:
   ```
   https://api.telegram.org/bot<TOKEN>/getUpdates
   ```

### 4. Настройка гостевого доступа

Скопируйте файл примера:
```bash
cp guest_access.example.json guest_access.json
```

## 📄 Описание файлов

- `bot_config.example.json` - Пример настроек бота (публичный)
- `bot_responses.json` - Тексты сообщений бота (публичный)
- `beta_warning.json` - Предупреждения бета-версии (публичный)
- `update_config.py` - Настройки системы обновлений (публичный)

**Приватные файлы (не в Git):**
- `bot_config.json` - Реальные настройки бота
- `guest_access.json` - Данные гостевого доступа

## ⚠️ Важно

НЕ публикуйте файлы с реальными токенами и секретными данными!
